import { SMButton } from "./shared";

const AppleOAuth = () => {
  return (
    <SMButton
      label="Continue with Apple"
      onPress={() => {}}
      backgroundColor="#111"
      color="#fff"
    />
  );
};

export default AppleOAuth;
